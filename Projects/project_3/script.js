"use strict";

/*let w = +prompt('Введите количевство килограм(от 1 до 100)', '');

if(w <= 100){
    if(w % 2 == 0){
    console.log('Yes');
} else{
    console.log('No');
}
}else{
    console.log('Error');
}

let m = +prompt('Введите размер доски(высота):', ''),
    n = +prompt('Введите размер доски(длина):', '');

if(n > 16 || m > n || m < 1 || n < 1){
    console.log('Error');
} else{
    console.log(m);
    console.log(n);
}

console.log(Math.floor((m*n)/2));

function getMathResult(num1, num2){
    if(typeof(num2) !== Number|| num2 <= 0){
        return num1;
    }else{
        console.log(num2);
    }
    return;
}

getMathResult(2, 4);

function someShit(length){
    if(typeof(length) !== 'number' || length < 0 || !Number.isInteger(length)){
        return console.log('При вычислении произошла ошибка');
}
    
    let w = length*length*length,
          s = 6*(length*length);
    
    return console.log(`Объём куба:${w}, площадь всей поверхности:${s}`);
}

someShit(12);

function iDoNotGiveAFuck(num){
    if(typeof(num) !== 'number' || num < 0 || !Number.isInteger(num)){
        return console.log('Ошибка. Проверьте правильность веденного номера места');
    } else if(num === 0 || num > 36){
        return console.log('Таких мест в вагоне не существует');
    }

    for(let i = 4; i <= 36; i = 4 + i){

    }
}

iDoNotGiveAFuck(1000);


function findMaxNumber(firstNum, secondNum, thirdNum, fourthNum){
    if(typeof(firstNum, secondNum, thirdNum, fourthNum) !== 'number'){
        return console.log('0');
    }

    return console.log(Math.max(firstNum, secondNum, thirdNum, fourthNum));

}

findMaxNumber(2, 4, 12, 'asd');*/

